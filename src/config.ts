const config = {
  basename: "/playground",
  defaultPath: "/dashboard/default",
  fontFamily: `'Product Sans'`,
  borderRadius: 12,
  theme: "light" as "light" | "dark",
  i18n: "en"
};
export default config;
